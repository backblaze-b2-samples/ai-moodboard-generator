{
  "name": "ai-moodboard-generator",
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "doctor": "node scripts/doctor.mjs",
    "predev": "node scripts/doctor.mjs",
    "dev": "sh scripts/dev.sh",
    "dev:web": "pnpm --filter @ai-moodboard-generator/web dev",
    "dev:api": "cd services/api && .venv/bin/uvicorn main:app --reload --port ${API_PORT:-8000}",
    "build": "pnpm --filter @ai-moodboard-generator/web build",
    "typecheck": "pnpm --filter @ai-moodboard-generator/web typecheck",
    "lint": "pnpm --filter @ai-moodboard-generator/web lint",
    "lint:api": "cd services/api && .venv/bin/ruff check .",
    "test:api": "cd services/api && .venv/bin/python -m pytest",
    "check:structure": "cd services/api && .venv/bin/python -m pytest tests/test_structure.py -v",
    "test:e2e": "pnpm --filter @ai-moodboard-generator/web exec playwright test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20.19.0",
    "pnpm": ">=10.24.0"
  }
}
