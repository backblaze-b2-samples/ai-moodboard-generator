fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0
python-magic>=0.4.27
Pillow>=11.0.0
PyPDF2>=3.0.0

# AI image generation via Genblaze (provider orchestration). The OpenAI
# DalleProvider drives gpt-image-1; swap in genblaze-replicate / etc. to
# change providers without touching the rest of the app.
genblaze-core>=0.3.0
genblaze-openai>=0.3.0
# httpx is used by the Genblaze repo adapter to fetch provider CDN URLs
# (dall-e family) and is also the test client — runtime dependency now.
httpx>=0.27.0

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
